/**
 * Generated by Apache Royale Compiler from org/apache/royale/language/toAttributeName.as
 * org.apache.royale.language.toAttributeName
 *
 * @fileoverview
 *
 * @suppress {missingRequire|checkTypes|accessControls}
 */

goog.provide('org.apache.royale.language.toAttributeName');
/* Royale Dependency List: QName,org.apache.royale.utils.Language,XML*/




/**
 * @param {*} name
 * @return {QName}
 */
org.apache.royale.language.toAttributeName = function(name) {
  
/**
 * @const
 * @type {string}
 */
var typeName = typeof(name);
  if (name == null || typeName == 'number' || typeName == 'boolean') {
    throw new TypeError('invalid xml name');
  }
  var /** @type {QName} */ qname;
  if (org.apache.royale.utils.Language.is(name, QName)) {
    if (org.apache.royale.utils.Language.as(name, QName, true).isAttribute)
      qname = org.apache.royale.utils.Language.as(name, QName); else {
      qname = new QName(name);
      qname.setIsAttribute(true);
    }
  } else {
    var /** @type {string} */ str = name.toString();
    var /** @type {number} */ idx = (str.indexOf('@')) >> 0;
    if (idx != -1)
      str = str.slice(idx + 1);
    qname = new QName(str);
    qname.setIsAttribute(true);
  }
  return qname;
}
