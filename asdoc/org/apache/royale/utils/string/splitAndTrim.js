/**
 * Generated by Apache Royale Compiler from org/apache/royale/utils/string/splitAndTrim.as
 * org.apache.royale.utils.string.splitAndTrim
 *
 * @fileoverview
 *
 * @suppress {missingRequire|checkTypes|accessControls}
 */

goog.provide('org.apache.royale.utils.string.splitAndTrim');
/* Royale Dependency List: org.apache.royale.utils.string.trim*/




/**
 *  Removes all whitespace characters from the beginning and end
 *  of each element in an Array, where the Array is stored as a String. 
 *
 *  @asparam value The String whose whitespace should be trimmed. 
 *
 *  @asparam separator The String that delimits each Array element in the string.
 *
 *  @asreturn Array where whitespace was removed from the 
 *  beginning and end of each element. 
 *  
 *  @langversion 3.0
 *  @playerversion Flash 10.2
 *  @playerversion AIR 2.6
 *  @productversion Royale 0.9.7
 *  @royalesuppressexport
 * @param {string} value
 * @param {string} delimiter
 * @return {Array}
 */
org.apache.royale.utils.string.splitAndTrim = function(value, delimiter) {
  if (value != "" && value != null) {
    var /** @type {Array} */ items = value.split(delimiter);
    var /** @type {number} */ len = (items.length) >> 0;
    for (var /** @type {number} */ i = 0; i < len; i++) {
      items[i] = org.apache.royale.utils.string.trim(org.apache.royale.utils.Language.string(items[i]));
    }
    return items;
  }
  return [];
}
