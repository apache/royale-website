{
  "name": "royale-11ty",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx rimraf _site && npx @11ty/eleventy",
    "start": "npx @11ty/eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-plugin-rss": "^2.0.4",
    "clean-css": "^5.3.3",
    "html-minifier": "^4.0.0",
    "rimraf": "^6.0.1",
    "sass": "^1.93.2"
  }
}
